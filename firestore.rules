rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMP: allow all. DO NOT use long-term.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
